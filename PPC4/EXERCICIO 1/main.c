/**
Implementar o TAD lista usando alocação dinâmica com encadeamento duplo.
Operações que o TAD deve contemplar:
• - Inicializar lista
• - Verificar lista vazia
• - Inserir elemento
• -Remover elemento
• -Tamanho: retorna o número de elementos da lista
• -Média: retornar a média aritmética simples dos elementos da lista
• - Iguais: recebe duas listas ordenadas e verifica se elas são iguais
• -Remover todos: remove todas as ocorrências de um elemento em uma lista
• -Remover maior: remove o maior elemento da lista encontrado. Remova
todas as ocorrências deste elemento.
• -Multiplos de 3: retornar uma lista L2 formada pelos elementos da lista de
entrada L, que são múltiplos de 3.
**/

int int main(int argc, char const *argv[])
{
	novaLista = inicia_lista();

	printf("tamanho da lista: %d\n",tamanho(novaLista) );
	return 0;
}